# React App

## Local Development
1. Install Node.js
2. Install dependencies: `npm install`
3. Start app: `npm start`
4. Open: http://localhost:3000

## Production Build
1. Build: `npm run build`
2. Files will be in `build/` directory

## Deployment
Push to main branch → Automatic deployment to AWS